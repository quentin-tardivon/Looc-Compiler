//Ligne 9 : type de retour de la méthode ne correspond pas au type de la valeur retournée
class Math =
(
	method pow(a : int, b : int) : string {
		var retval : int;

		retval := 1;

		return (retval);        //retval est du type int, le type de retour de pow est string
	}

)

var m : Math;
m := new Math;
write m.michel(2, 3);