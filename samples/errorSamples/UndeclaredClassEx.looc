class Animal = (

    var name : string ;

    method setName (n: string) {
      name := n;
       }

    method whoami() {
        write "My name is";
        write name;
        }
)

class Dog inherit Animal = ()

class Cat inherit Animal = ()

var a1 : Animal;

var a2 : Animal;

a1 := new Michel;

do a1.setName("boo") ;

a2 := new Cat ;
