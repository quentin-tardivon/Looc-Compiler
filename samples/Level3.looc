class Math = (
    var pi: int;
    var pi2: int;

    method calcPi() {
        write 34;
    }
)

class Square = (
    var size: int;
    var perimeter: int;
    var area: int;

    method init() {
        size := 3;
    }

    method getSize(): int {
        return (size);
    }
)

var m : Math;
m := new Math;
do m.calcPi();