class Counter = (
    var count : int;

    method init(i: int) {
        count := i;
    }

    method incrCount(): int {
        count := count + 1;
        return (count);
    }

)

var counter: Counter;
counter := new Counter;
do counter.init(10);
write counter.incrCount();