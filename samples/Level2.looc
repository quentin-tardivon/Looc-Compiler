class Math =
(

    var pi: int;

	method pow(a : int, b : int) : int {
		var retval : int;
		var i : int;

		/*if b == 0 then
			return (1);
        else
            return (2);
		fi*/

		retval := 1 * 3 + 2;
		/*for i in 1 .. b
		do
			retval := retval * a;
		end*/

		return (retval);
	}

)


var m : Math;
//m := new Math;
write m.pow(2, 3);
