//pow n'est pas appelé avec les bons paramètres (type)
//ligne 27

class Math =
(
	method pow(a : string, b : string) : int {
		var retval : int;
		var i : int;
		if b == 0 then
			return (1);
		fi
		retval := 1;

		return (retval);
	}

)


var m : Math;
var n : int;
var res : int;
n := 3;
m := new Math;
res := m.pow(3,3);