
class Math = (
	method pow(a : int, b : int) : string{
		var retval : int;
		var i : int;

		if b == 0 then
			return (1);
		fi
		retval := 1;
		for i in 1 .. b
		do
			retval := retval * a;
		end

		return ("cat");
	}



)
var m : Math;
var n : int;
m := new Math;
n := 1 + m.pow(3,3);



